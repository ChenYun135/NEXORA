# Data Freshness / 数据新鲜度

OpenAlex: monthly snapshot; World Bank: annual data with quarterly metadata check; USPTO: monthly when configured; Data.gov catalog: weekly; policy: weekly/manual status verification; organization identity: monthly/manual. Stale state is explicit. A failed refresh cannot replace last-known-good data with empty results.
