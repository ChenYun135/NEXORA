# Public research exports

This directory contains aggregate, public-safe and reproducible provider metadata. It intentionally excludes unpublished research, restricted microdata, credentials and local paths.

- `provider-registry.json` — public provider planning metadata.

These artifacts are specifications or aggregates, not forecasts or causal estimates.
